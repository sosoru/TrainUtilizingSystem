<UserControl
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SensorLivetView.Views" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:SensorLivetView_ViewModels="clr-namespace:SensorLivetView.ViewModels.Controls" mc:Ignorable="d" x:Class="SensorLivetView.Views.TrainSensorView"
        >
    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
    	<!-- <StackPanel Orientation="Horizontal">
    		<TextBlock Text="CurrentVoltage:"/>
    		<TextBlock Text="{Binding Model.CurrentState.CurrentVoltage}"/>
    	</StackPanel> -->
    	<StackPanel Orientation="Horizontal" >
    		<TextBlock Text="Mode:"/>
    		<TextBlock Text="{Binding Model.CurrentState.Mode}"/>
    	</StackPanel>
    	<StackPanel Orientation="Horizontal">
    		<TextBlock Text="ThretholdVoltage:"/>
			<TextBox x:Name="textBox" Text="{Binding Model.CurrentState.ThresholdVoltage}"></TextBox>
    	</StackPanel>
    	<Button Content="Button" Command="{Binding ChangeMeisuringModeCommand, Mode=OneWay}"/>
    	<Button Content="Button" Command="{Binding ChangeDetectingModeCommand, Mode=OneWay}" CommandParameter="{Binding Text, ElementName=textBox}"/>
    	<local:VoltageGraphView Height="200" Width="200" DataContext="{Binding VoltageGraphVm, Mode=OneWay}"/>
		
	</StackPanel>
</UserControl>
